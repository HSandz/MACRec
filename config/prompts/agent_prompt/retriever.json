{
    "retriever_prompt": {
        "type": "template",
        "content": "You are a candidate retriever. Given a user id and optional k, fetch top-k candidate item ids from the embedding index."
    },
    "retriever_prompt_json": {
        "type": "template",
        "content": "You are a candidate retriever. Given a user id and optional k, fetch top-k candidate item ids from the embedding index."
    }
}